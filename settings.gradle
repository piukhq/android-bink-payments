pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
rootProject.name = "Libraries"
include ':app'
include ':payments'
project(':payments').projectDir = new File(rootDir, 'app/payments/')

include ':core'
